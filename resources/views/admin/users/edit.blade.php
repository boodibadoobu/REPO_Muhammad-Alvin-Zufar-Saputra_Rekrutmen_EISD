@extends('layouts.app')

@section('title', 'Edit Pengguna')

@section('content')
    <section class="page-section"><div class="container narrow-container"><div class="breadcrumb"><a href="{{ route('admin.users.index') }}">Pengguna</a><span>/</span><span>Edit</span></div><div class="page-heading"><span class="eyebrow">Hak akses</span><h1>Kelola pengguna</h1><p>Pastikan role sesuai tanggung jawab pengguna dalam sistem.</p></div><form method="POST" action="{{ route('admin.users.update', $managedUser) }}" class="form-panel">@csrf @method('PUT')<div class="form-stack"><label class="field"><span>Nama lengkap</span><input type="text" name="name" value="{{ old('name', $managedUser->name) }}" required maxlength="100">@error('name')<small class="field-error">{{ $message }}</small>@enderror</label><label class="field"><span>Email</span><input type="email" name="email" value="{{ old('email', $managedUser->email) }}" required>@error('email')<small class="field-error">{{ $message }}</small>@enderror</label><label class="field"><span>Role</span><select name="role" required>@foreach($roles as $role)<option value="{{ $role->value }}" @selected(old('role', $managedUser->role->value) === $role->value)>{{ $role->label() }}</option>@endforeach</select>@error('role')<small class="field-error">{{ $message }}</small>@enderror</label><div class="info-box"><strong>Pembagian akses</strong><p>Warga membuat laporan, petugas memproses laporan, dan admin juga mengelola pengguna serta kategori.</p></div></div><div class="form-actions"><a href="{{ route('admin.users.index') }}" class="button button-ghost">Batal</a><button type="submit" class="button button-primary">Simpan pengguna</button></div></form></div></section>
@endsection
